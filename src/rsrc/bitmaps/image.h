#ifndef _IMAGE_H
#define _IMAGE_H_

#include <stdint.h>

/* Definitions for what's produced by bmp2c, the small utility that converts a picture into C with RLE compression. */

extern uint8_t splashscreen_lut[];
extern uint8_t splashscreen_pix[];

#endif
